# HeyU

##command for install bower
```  bower install ```

###install sass   
#####( only if you not install sass before )
``` gem install sass ```

###install watch  
#####( only if you not install sass before )
``` gem install compass ```

##compile scss command
``` compass watch ```